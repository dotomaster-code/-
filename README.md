# I-BRIDGE 계정 관리 시스템

🌉 I-BRIDGE는 사용자 계정을 효율적으로 관리할 수 있는 웹 기반 시스템입니다.

## 주요 기능

- ✨ **사용자 목록 조회**: 모든 계정을 한눈에 확인
- ➕ **계정 추가**: 새로운 사용자 계정 생성
- ✏️ **계정 수정**: 기존 계정 정보 업데이트
- 🗑️ **계정 삭제**: 불필요한 계정 제거
- 🔍 **실시간 검색**: 사용자명 또는 이메일로 빠른 검색
- 📊 **통계 대시보드**: 전체/활성/비활성 계정 현황

## 설치 및 실행

### 필수 요구사항
- Node.js 14.0 이상

### 설치
```bash
npm install
```

### 실행
```bash
npm start
```

서버가 3000번 포트에서 실행됩니다.

## 사용 방법

1. **계정 추가**: 우측 상단 "새 계정 추가" 버튼 클릭
2. **계정 수정**: 각 계정 행의 "수정" 버튼 클릭
3. **계정 삭제**: 각 계정 행의 "삭제" 버튼 클릭 (확인 필요)
4. **검색**: 상단 검색창에서 사용자명 또는 이메일로 검색

## 프로젝트 구조

```
i-bridge/
├── server.js           # Express 서버 및 API
├── package.json        # 프로젝트 설정
├── public/
│   ├── index.html      # 메인 HTML
│   ├── styles.css      # 스타일시트
│   └── script.js       # 클라이언트 JavaScript
└── README.md           # 프로젝트 문서
```

## API 엔드포인트

- `GET /api/users` - 모든 사용자 조회
- `GET /api/users/:id` - 특정 사용자 조회
- `POST /api/users` - 새 사용자 추가
- `PUT /api/users/:id` - 사용자 정보 수정
- `DELETE /api/users/:id` - 사용자 삭제

## 기술 스택

- **Backend**: Node.js + Express
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **스타일**: 그라디언트 디자인, 반응형 레이아웃

## 라이선스

MIT License
